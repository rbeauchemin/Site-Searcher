To run this one, just open it up and change the top two variable calls. Python calls the first index 0, so if you want to select the first column, set that value as [0], second is [1], etc.
After, go to your trusty command prompt and type:

python url_checker.py

This might take some time...

Cheers!
Ryan